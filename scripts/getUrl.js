/*const getUrl = async(url)=>{
    console.log(url) 
    try {
        const  resp = await fetch(url)
    const data = await resp.json()
    return(data); 
    }
    catch (error) {  
        throw error    
    }};
    export default getUrl*/

   